build/
build_esp32/
build_log.md
*.bin
# Regenerate sdkconfig from the sdkconfig.defaults* files.
sdkconfig.esp32
sdkconfig.esp32c6
sdkconfig.*.old
sdkconfig.old
managed_components/

# Mosquitto deploy pack: never commit private keys or editor backups
certs/config/*.key
certs/config/*.save
certs/config/*.save.*
